# dependencies / build
backend/.venv/
frontend/node_modules/
frontend/dist/

# runtime data (uploads, SQLite, signed PDFs)
backend/data/

# caches
backend/**/__pycache__/
backend/.pytest_cache/
frontend/node_modules/.package-lock.json

# test fixtures — sign-in sheets with personal names; sample_signbook.pdf is
# regenerated with `python -m tests.make_sample`
backend/tests/fixtures/*.pdf

# local editor / tool settings
.claude/settings.local.json
.DS_Store
*.log
